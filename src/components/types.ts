export type ButtonVariant = 'primary' | 'secondary';
export type TextFieldVariant = 'primary' | 'secondary' | 'error';

interface IconProps {
  size?: number;
  color?: string;
  className?: string;
}
