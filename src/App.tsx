import React from 'react';
import Button from './components/Button/Button';

const App: React.FC = () => {
  return (
    <div className="box">
      <Button variant="primary">
        Submit
      </Button>
    </div>
  );
};

export default App;
